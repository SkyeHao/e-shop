server:
  port: 8081
spring:
  application:
    name: product
  profiles:
    active: datasource,regist,mybatis,minio
  cloud:
    nacos:
      config:
        server-addr: 192.168.40.100:8848
        prefix: hsk
        file-extension: yml
